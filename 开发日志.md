



# 时间线
## 2025年 9月1日 - 20259月7日 融合agent reAct方法使得模型能够自我实现一个代码系统

1. 前端目前是一个对话系统，为了将代码可以展示，用户可以阅览，所以重新开一个页面专门显示代码，用户可以点击❌，返回原来的对话界面；
2. 代码系统可以查看项目代码，可以下载整个项目的安装包；
3、为了将这段代码保存进历史，我们还是将文本加入了上下文，但是为了保证读者每一个都能读到以前写的代码，所以我们会在对话框里面显示
一个代码，按钮点击就可以打开刚才的代码界面，查看前面写的代码；


具体实现的todolist:
1. 修改前端可以支持deepseek-reasoner的agent模型，添加一个模式选择，添加上agent选项，目前不具备
2. 实现多轮对话功能，因为在生成代码的过程中需要用户确认关键的操作，所以可以需要停下来确认，这是模型生成其实没有结束
所以用户还不能输入，所以我们需要在返回的数据中加入特殊结束符，表示是否模型真的结束了
3.代码展示界面用户能够下拉查看代码，


2025.9.8 
1、实现生成代码展示（目前是默认文件夹）
2、用户的对话记录显示结果，并且提供一个类似于按钮的东西可以点击查看代码



2025.12.1（已解决）
1、Bug：发现多个用户共享一个session，问题已经发现：后端的bearer token给每一个用户是一致的，所以信息会重复【已修复】
这个原来是一个伪需求，我自己查看了qwen chat用一个浏览器同时只允许登陆一个账号

2025.12.4（已解决）
2、发现了一个边缘bug，如果有一页没有任何东西会报错，原因是：作者首先会求出整个ppt的layout布局，然后聚类，最终选出几个类别的layout，而


2025.12.4（未解决）
3、作者在用大模型生成replace_picture的时候是看不到备用的图片有哪些的，替换用的图片路径找不到会报错。
在pptgen.py里面
 turn_id, edit_actions = await self.staffs["coder"](
            api_docs=code_executor.get_apis_docs(API_TYPES.Agent.value),
            edit_target=self.presentation.slides[template_id - 1].to_html(),
            command_list="\n".join([str(i) for i in command_list]),
        )
    第一个函数是对每一个可以操作的函数的解析；
    第二个是当前被编辑slide的html文件；
    第三个是目前这个ppt应该采取哪些命令进行修改；
# NEWS

## [2025/9/14] 实现了ai对话地流式传输
## [2025/10/2] 学习了PPT的多智能体生成技术方案

